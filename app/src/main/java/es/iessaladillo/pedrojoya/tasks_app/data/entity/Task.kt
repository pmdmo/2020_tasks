package es.iessaladillo.pedrojoya.tasks_app.data.entity

// TODO: DEFINIR UNA TAREA CON LOS SIGUIENTES CAMPOS:
// id (long), concept (cadena), category (cadena), createdAt (cadena),
// completed (booleano), completedAt (cadena)
class Task
